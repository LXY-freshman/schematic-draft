/**
 * Schematic Draft — an offline desktop schematic editor.
 *
 * Everything the editor reads or writes lives on this computer. There is no
 * account, no Gallery, no analytics and no hosted simulation, so the product
 * name is a constant rather than something a deployment configures.
 */
export const PRODUCT_NAME = "Schematic Draft";
